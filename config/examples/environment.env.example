# Vortex Environment Variables Example
# Copy this to .env in the root directory and customize for your needs
#
# RECOMMENDATION: Use TOML configuration (config/config.toml) instead of environment variables
# TOML files are more readable and maintainable for complex configurations
#
# Configuration precedence: Environment Variables > TOML Configuration > Application Defaults

# ============================================================================
# GENERAL CONFIGURATION
# ============================================================================

# Default data provider: yahoo (free), barchart (premium), ibkr (professional)
VORTEX_DEFAULT_PROVIDER="yahoo"

# Output directory for downloaded data
VORTEX_OUTPUT_DIR="./data"

# Enable backup files in Parquet format
VORTEX_BACKUP_ENABLED="false"

# Perform dry run without actually downloading
VORTEX_DRY_RUN="false"

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Logging level: DEBUG, INFO, WARNING, ERROR
VORTEX_LOGGING_LEVEL="INFO"

# Log format: console, json, rich
VORTEX_LOGGING_FORMAT="console"

# Log output destinations: console, file, or both (comma-separated)
VORTEX_LOGGING_OUTPUT="console"

# Log file path (when output includes file)
VORTEX_LOGGING_FILE_PATH="./logs/vortex.log"

# ============================================================================
# PROVIDER CREDENTIALS
# ============================================================================

# Barchart.com (Premium data - requires subscription)
VORTEX_BARCHART_USERNAME="your_email@example.com"
VORTEX_BARCHART_PASSWORD="your_password"
VORTEX_BARCHART_DAILY_LIMIT="150"

# Interactive Brokers (Requires TWS/Gateway running)
VORTEX_IBKR_HOST="localhost"
VORTEX_IBKR_PORT="7497"
VORTEX_IBKR_CLIENT_ID="1"

# Yahoo Finance (Free data - no credentials required)
# No environment variables needed

# ============================================================================
# DOCKER-SPECIFIC SETTINGS
# ============================================================================

# Container runtime settings (for Docker Compose)
VORTEX_SCHEDULE="0 8 * * *"              # Cron schedule for downloads
VORTEX_RUN_ON_STARTUP="true"             # Run download when container starts
VORTEX_DOWNLOAD_ARGS="--yes"             # Additional arguments for download command

# Host directory mappings (for Docker Compose)
DATA_DIR="./data"                        # Host directory for downloaded data
CONFIG_DIR="./config"                    # Host directory for configuration

# ============================================================================
# EXAMPLE CONFIGURATIONS
# ============================================================================

# Example 1: Free Yahoo Finance (default - no credentials needed)
# VORTEX_DEFAULT_PROVIDER="yahoo"

# Example 2: Premium Barchart
# VORTEX_DEFAULT_PROVIDER="barchart"
# VORTEX_BARCHART_USERNAME="your_email@example.com"
# VORTEX_BARCHART_PASSWORD="your_password"
# VORTEX_BACKUP_ENABLED="true"

# Example 3: Interactive Brokers
# VORTEX_DEFAULT_PROVIDER="ibkr" 
# VORTEX_IBKR_HOST="host.docker.internal"  # Use this in Docker to access host TWS
# VORTEX_IBKR_PORT="7497"
# VORTEX_LOGGING_LEVEL="DEBUG"

# Example 4: Development/Testing
# VORTEX_DRY_RUN="true"
# VORTEX_LOGGING_LEVEL="DEBUG"
# VORTEX_LOGGING_FORMAT="rich"